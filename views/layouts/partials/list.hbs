<div class="board-header">
  <h1>게시판</h1>
  <div class="board-actions">
    <div class="search-box">
      <input type="search" placeholder="검색어를 입력하세요" />
      <button class="btn-search">검색</button>
    </div>
    <a href="/write" class="btn-primary">글쓰기</a>
  </div>
</div>
{{#each data}}
  <div class="board-list">
    <div class="post-item">
      <div class="post-header">
        <h3 class="post-title">
          <a href="/posts/{{id}}">{{title}}</a>
        </h3>
        <span class="post-meta">{{createdAt}}</span>
      </div>
      <p class="post-excerpt">{{title}}</p>
      <div class="post-footer">
        <span class="post-stats">댓글 {{comments.length}}</span>
        <span class="post-stats">조회 0</span>
      </div>
    </div>
{{/each}}
<div class="pagination">  
  <button class="page-btn" {{#unless hasPreviousPage}}disabled{{/unless}}>이전</button>
  <div class="page-numbers">
    <span class="page-number active">1</span>
    <span class="page-number">2</span>
    <span class="page-number">3</span>
    <span class="page-number">4</span>
    <span class="page-number">5</span>
  </div>
  <button class="page-btn" {{#unless hasNextPage}}disabled{{/unless}}>다음</button>
</div>
<style>
  .board-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 24px;
    flex-wrap: wrap;
    gap: 16px;
  }

  .board-header h1 {
    margin: 0;
    font-size: 24px;
    font-weight: 700;
    color: var(--text);
  }

  .board-actions {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .search-box {
    display: flex;
    align-items: center;
    gap: 8px;
    background: white;
    padding: 6px 12px;
    border-radius: 8px;
    border: 1px solid rgba(15, 23, 42, 0.08);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
  }

  .search-box input {
    border: none;
    outline: none;
    padding: 6px;
    font-size: 14px;
    min-width: 200px;
  }

  .btn-search {
    background: var(--accent);
    color: white;
    border: none;
    padding: 6px 14px;
    border-radius: 6px;
    font-weight: 600;
    cursor: pointer;
    font-size: 14px;
    transition: all 0.2s;
  }

  .btn-search:hover {
    background: #1d4ed8;
  }

  .btn-primary {
    background: var(--accent);
    color: white;
    padding: 8px 18px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.2s;
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }

  .btn-primary:hover {
    background: #1d4ed8;
    transform: translateY(-1px);
  }

  .board-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .post-item {
    background: white;
    padding: 20px;
    border-radius: 10px;
    border: 1px solid rgba(15, 23, 42, 0.06);
    transition: all 0.2s;
    cursor: pointer;
  }

  .post-item:hover {
    border-color: var(--accent);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.08);
    transform: translateY(-2px);
  }

  .post-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
    gap: 12px;
  }

  .post-title {
    margin: 0;
    font-size: 17px;
    font-weight: 700;
  }

  .post-title a {
    color: var(--text);
    text-decoration: none;
    transition: color 0.2s;
  }

  .post-title a:hover {
    color: var(--accent);
  }

  .post-meta {
    color: var(--muted);
    font-size: 13px;
    white-space: nowrap;
  }

  .post-excerpt {
    margin: 0 0 12px;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .post-footer {
    display: flex;
    gap: 16px;
    padding-top: 8px;
    border-top: 1px solid rgba(15, 23, 42, 0.04);
  }

  .post-stats {
    color: var(--muted);
    font-size: 13px;
  }

  .pagination {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin-top: 32px;
  }

  .page-btn {
    background: white;
    border: 1px solid rgba(15, 23, 42, 0.08);
    padding: 8px 16px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.2s;
  }

  .page-btn:hover:not(:disabled) {
    background: var(--accent);
    color: white;
    border-color: var(--accent);
  }

  .page-btn:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }

  .page-numbers {
    display: flex;
    gap: 6px;
  }

  .page-number {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 600;
    font-size: 14px;
    transition: all 0.2s;
    border: 1px solid transparent;
  }

  .page-number:hover {
    background: rgba(37, 99, 235, 0.1);
    color: var(--accent);
  }

  .page-number.active {
    background: var(--accent);
    color: white;
    border-color: var(--accent);
  }

  @media (max-width: 640px) {
    .board-header {
      flex-direction: column;
      align-items: stretch;
    }

    .board-actions {
      flex-direction: column;
    }

    .search-box {
      width: 100%;
    }

    .search-box input {
      flex: 1;
      min-width: auto;
    }

    .post-header {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
{{!-- <div style="display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:14px;flex-wrap:wrap;">
  <h1 style="margin:0;font-size:20px;">게시판</h1>

  <div style="display:flex;gap:8px;align-items:center;">
    <form method="GET" action="/posts" style="display:flex;gap:8px;align-items:center;">
      <input type="text" name="search" value="{{search}}" placeholder="제목으로 검색" style="padding:8px 10px;border-radius:8px;border:1px solid rgba(15,23,42,0.06);min-width:220px;">
      <input type="hidden" name="limit" value="{{limit}}">
      <button type="submit" style="padding:8px 12px;border-radius:8px;background:#2563eb;color:#fff;border:0;cursor:pointer;">검색</button>
    </form>

    <a href="/write" style="padding:8px 12px;border-radius:8px;background:transparent;border:1px solid rgba(37,99,235,0.12);color:#2563eb;text-decoration:none;font-weight:600;">새 글</a>
  </div>
</div>

<div style="display:grid;gap:10px;">
  {{#each data}}
    <article style="display:flex;flex-direction:column;background:transparent;padding:12px;border-radius:8px;border:1px solid rgba(15,23,42,0.04);">
      <div style="display:flex;align-items:center;justify-content:space-between;gap:12px;">
        <a href="/posts/{{this.id}}" style="font-weight:700;color:var(--text);text-decoration:none;font-size:16px;">{{this.title}}</a>
        <time style="color:var(--muted);font-size:13px;">{{this.createdAt}}</time>
      </div>
      {{#if this.excerpt}}
        <p style="margin:8px 0 0;color:var(--muted);font-size:14px;line-height:1.4;">{{this.excerpt}}</p>
      {{/if}}
    </article>
  {{/each}}

  {{#unless data.length}}
    <div style="text-align:center;padding:28px;border-radius:8px;border:1px dashed rgba(15,23,42,0.04);color:var(--muted);">게시글이 없습니다.</div>
  {{/unless}}
</div>

<div style="display:flex;align-items:center;justify-content:center;gap:12px;margin-top:14px;flex-wrap:wrap;">
  {{#if (gt page 1)}}
    <a href="/posts?page={{dec page}}&limit={{limit}}&search={{search}}" style="padding:8px 12px;border-radius:8px;background:#fff;border:1px solid rgba(15,23,42,0.06);text-decoration:none;color:var(--text);">이전</a>
  {{/if}}

  <div style="padding:6px 10px;border-radius:999px;background:rgba(15,23,42,0.03);font-weight:600;">
    {{page}} / {{totalPages}}
  </div>

  {{#if (lt page totalPages)}}
    <a href="/posts?page={{inc page}}&limit={{limit}}&search={{search}}" style="padding:8px 12px;border-radius:8px;background:#2563eb;color:#fff;text-decoration:none;">다음</a>
  {{/if}}
</div>

<hr style="margin:20px 0;border:0;border-top:1px solid rgba(15,23,42,0.04);">

<h2 style="margin-top:0;">새 글 작성</h2>
<form method="POST" action="/posts" style="display:flex;flex-direction:column;gap:8px;">
  <input type="text" name="title" placeholder="제목" required maxlength="100" style="padding:10px;border-radius:8px;border:1px solid rgba(15,23,42,0.06);width:100%;">
  <textarea name="content" placeholder="내용" required style="padding:10px;border-radius:8px;border:1px solid rgba(15,23,42,0.06);width:100%;height:150px;"></textarea>
  <div style="display:flex;gap:8px;justify-content:flex-end;">
    <button type="submit" style="padding:8px 12px;border-radius:8px;background:#2563eb;color:#fff;border:0;cursor:pointer;">등록</button>
  </div>
</form> --}}